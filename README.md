## README ##

To run this project, you will need `uv` installed, and then run these commands:

```sh
# clone the repo
git clone git@github.com:24393-school/12DTS.git
cd 12DTS/Runeforge

# build virtual environment
uv sync
```

To run the program:

```sh
uv run main.py
```
