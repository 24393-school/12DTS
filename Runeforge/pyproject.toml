[project]
name = "runeforge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rich (>=14, <15)",
    "word2number (>=1.1, <2)",
]
